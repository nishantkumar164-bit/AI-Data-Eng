-- Joins: write INNER, LEFT, SELF, and FULL OUTER JOIN examples.
-- Explain grain and calculate duplicate-key join row counts.
-- Explain NULL join behavior.
-- Prevent double-counting order amounts after joining order_items.

-- Window Functions: customer totals, running totals, rankings, top 3 per department,
-- LAG, LEAD, and filtering window results with a CTE.

-- CTEs: find customers spending over 1000; use two CTEs to find customers
-- spending over 5000 with at least 3 orders; make one CTE reference another;
-- explain CTE versus subquery; correct missing commas, GROUP BY, aliases, and AND.
